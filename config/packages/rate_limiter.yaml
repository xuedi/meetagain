framework:
    rate_limiter:
        password_reset:
            policy: 'fixed_window'
            limit: 10
            interval: '1 hour'
        registration:
            policy: 'fixed_window'
            limit: 10
            interval: '1 hour'
