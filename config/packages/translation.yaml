framework:
    default_locale: en
    # enabled_locales are now managed via database (see LanguageService)
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en
        providers:
