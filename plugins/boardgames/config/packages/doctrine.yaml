doctrine:
    orm:
        entity_managers:
            default:
                mappings:
                    Boardgames:
                        dir: "%kernel.project_dir%/plugins/boardgames/src/Entity"
                        is_bundle: false
                        prefix: Plugin\Boardgames\Entity
                        alias: Boardgames

doctrine_migrations:
    migrations_paths:
        PluginBoardgamesMigrations: "%kernel.project_dir%/plugins/boardgames/migrations"
