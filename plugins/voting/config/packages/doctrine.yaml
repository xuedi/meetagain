doctrine:
    orm:
        entity_managers:
            default:
                mappings:
                    Voting:
                        dir: "%kernel.project_dir%/plugins/voting/src/Entity"
                        is_bundle: false
                        prefix: Plugin\Voting\Entity
                        alias: Voting

doctrine_migrations:
    migrations_paths:
        PluginVotingMigrations: "%kernel.project_dir%/plugins/voting/migrations"
