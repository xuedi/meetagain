doctrine:
    orm:
        entity_managers:
            default:
                mappings:
                    Ranking:
                        dir: "%kernel.project_dir%/plugins/ranking/src/Entity"
                        is_bundle: false
                        prefix: Plugin\Ranking\Entity
                        alias: Ranking

doctrine_migrations:
    migrations_paths:
        PluginRankingMigrations: "%kernel.project_dir%/plugins/ranking/migrations"
