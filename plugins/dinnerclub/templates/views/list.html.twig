<table id="filteredTable" class="table is-fullwidth">
    <thead>
    <tr>
        <th>Image</th>
        <th>Name</th>
        <th>Phonetic</th>
        <th>Description</th>
        <th>
            {% if is_granted('ROLE_USER') %}
                <a href="{{ path('plugin_dinnerclub_add') }}">
                    <span class="icon"><i class="fa fa-add"></i></span>
                </a>
            {% endif %}
        </th>
    </tr>
    </thead>
    <tbody>
    {% for item in list %}{# action items for managers #}
        <tr class="dish-item {% if editItem is defined and editItem.id == item.id %}has-background-light{% endif %}" data-dish-id="{{ item.id }}">
            <td>
                <a href="{{ path('plugin_dinnerclub_item_show', {'id': item.id}) }}">
                    {% include '_components/image.html.twig' with {'image': item.previewImage, 'size': '50x50'} %}
                </a>
            </td>
            <td>{{ item.getTranslatedName(app.locale) }}</td>
            <td>{{ item.getPhonetic(app.locale) }}</td>
            <td>{{ item.getTranslatedDescription(app.locale) }}</td>
            <td>
                {% if is_granted('ROLE_ORGANIZER') %}
                    <a href="{{ path('plugin_dinnerclub_edit', {'id': item.id}) }}">
                        <span class="icon"><i class="fa fa-edit"></i></span>
                    </a>
                {% endif %}
            </td>
        </tr>
    {% endfor %}
</table>
