{% extends 'base.html.twig' %}

{% block title %}{{ "cookie.page_title"|trans }}{% endblock %}
{% block meta_description %}{{ "cookie.meta_description"|trans }}{% endblock %}

{% block body %}

    <section class="section">
        <div class="container">
            <h1 class="title is-3">{{ "cookie.page_title" | trans }}</h1>

            <div class="columns">
                <div class="column is-8">
                    {{ form_start(form, {'attr': {'class': 'box'}}) }}
                        <p class="mb-4">{{ "cookie.consent_text" | trans }}</p>

                        <div class="field">
                            <label class="checkbox">
                                {{ form_widget(form.cookies) }}
                                <p class="help has-text-grey">{{ "cookie.cookies_text" | trans }}</p>
                            </label>
                        </div>

                        <div class="field">
                            <label class="checkbox">
                                {{ form_widget(form.osm) }}
                            </label>
                            <p class="help has-text-grey">{{ "cookie.option_osm_text" | trans }}</p>
                        </div>

                        <div class="field mt-4">
                            {{ form_widget(form.save) }}
                        </div>
                    {{ form_end(form) }}
                </div>

                <div class="column is-4">
                    <div class="box">
                        <p class="has-text-weight-bold mb-2">{{ "cookie.current_status"|trans }}</p>
                        <ul>
                            <li>
                                {{ "cookie.cookies_label"|trans }}:
                                {% if consent.cookies.value == 'granted' %}
                                    <span class="tag is-success">{{ "cookie.status_granted"|trans }}</span>
                                {% elseif consent.cookies.value == 'denied' %}
                                    <span class="tag is-danger">{{ "cookie.status_denied"|trans }}</span>
                                {% else %}
                                    <span class="tag is-warning">{{ "cookie.status_unknown"|trans }}</span>
                                {% endif %}
                            </li>
                            <li>
                                {{ "cookie.option_osm"|trans }}:
                                {% if consent.osm.value == 'granted' %}
                                    <span class="tag is-success">{{ "cookie.status_granted"|trans }}</span>
                                {% elseif consent.osm.value == 'denied' %}
                                    <span class="tag is-danger">{{ "cookie.status_denied"|trans }}</span>
                                {% else %}
                                    <span class="tag is-warning">{{ "cookie.status_unknown"|trans }}</span>
                                {% endif %}
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
