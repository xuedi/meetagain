{% extends 'base.html.twig' %}

{% block title %}{{ "page_title_login"|trans }}{% endblock %}

{% block body %}

    <section class="section">
        <div class="container">

            <div class="columns">
                <div class="column is-5">

                    <h1 class="title is-5">{{ "page_reset_password_headline"|trans }}</h1>

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="button">{{ "button_save" | trans }}</button>
                    {{ form_end(form) }}

                </div>

            </div>

        </div>
    </section>


{% endblock %}
