{% extends 'base.html.twig' %}

{% block title %}{{ "page_title_profile"|trans }}{% endblock %}

{% block body %}

    <section class="section is-hidden-touch">
        {% include 'profile/messages/chat_desktop.html.twig' %}
    </section>

    <section class="section is-hidden-desktop">
        {% include 'profile/messages/chat_mobile.html.twig' %}
    </section>

    {% if messages is not null %}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Scroll desktop message container to bottom
            const desktopContainer = document.getElementById('desktop-message-container');
            if (desktopContainer) {
                desktopContainer.scrollTop = desktopContainer.scrollHeight;
            }

            // Scroll mobile message container to bottom
            const mobileContainer = document.getElementById('mobile-message-container');
            if (mobileContainer) {
                mobileContainer.scrollTop = mobileContainer.scrollHeight;
            }
        });
    </script>
    {% endif %}
{% endblock %}
