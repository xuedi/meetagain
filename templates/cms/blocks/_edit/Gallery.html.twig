{% if block is not defined or editBlock == blockId %}
    <div class="field">
        <label class="label">Title</label>
        <div class="control">
            <input class="input" name="title" type="text"
                   value="{% if block is defined %}{{ block.title }}{% endif %}">
        </div>
    </div>
{% else %}
    {% if block.title %}
        <p class="has-text-weight-semibold">{{ block.title }}</p>
    {% endif %}
{% endif %}

{% if blockId is defined %}
    {% if block is defined and block.images|length > 0 %}
        <div class="columns is-multiline mt-2">
            {% for item in block.images %}
                <div class="column is-2">
                    <div style="position: relative; display: inline-block; width: 100%;">
                        <img src="/images/thumbnails/{{ item.hash }}_210x140.webp"
                             alt="" width="210" height="140"
                             style="object-fit: cover; width: 100%; display: block;">
                        <a href="{{ path('app_admin_cms_gallery_remove', {'blockId': blockId, 'imageId': item.id}) }}"
                           style="position: absolute; top: 4px; right: 4px; width: 24px; height: 24px; border-radius: 50%; background: rgba(220,53,69,0.85); color: #fff; display: flex; align-items: center; justify-content: center; font-size: 13px; line-height: 1; text-decoration: none;"
                           title="Remove">&#x2715;</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <p class="has-text-grey-light mt-2">No images yet.</p>
    {% endif %}

    <div class="mt-3">
        <a href="#"
           class="button is-success is-small modalTrigger"
           data-modal-url="{{ path('app_admin_cms_gallery_modal', {'blockId': blockId}) }}">
            <span class="icon"><i class="fa fa-plus"></i></span>
            <span>Add Images</span>
        </a>
    </div>
{% endif %}
