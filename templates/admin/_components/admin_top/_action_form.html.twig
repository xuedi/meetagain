<a href="{{ action.target }}"
   class="button is-small{% if action.variant %} {{ action.variant }}{% endif %}"
   data-post
   data-csrf-token="{{ csrf_token(action.csrfTokenId) }}"
   {%- if action.confirm %} data-confirm="{{ action.confirm }}"{% endif %}>
    {% if action.icon %}
        <span class="icon is-small"><i class="fa fa-{{ action.icon }}"></i></span>
    {% endif %}
    <span>{{ action.label }}</span>
</a>
