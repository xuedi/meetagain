{% extends 'admin/base.html.twig' %}

{% block content %}

    <div class="container">

        {% include 'admin/system/_tabs_navigation.html.twig' with {'activeSection': 'language'} %}

        <table id="filteredTable" class="table is-fullwidth">
            <thead>
            <tr>
                <th>{{ 'admin_system.col_flag'|trans }}</th>
                <th>{{ 'admin_system.col_code'|trans }}</th>
                <th>{{ 'admin_system.col_name'|trans }}</th>
                <th>{{ 'admin_system.col_has_tile'|trans }}</th>
                <th>{{ 'admin_system.col_status'|trans }}</th>
                <th>{{ 'admin_system.col_sort_order'|trans }}</th>
                <th>
                    <a href="{{ path('app_admin_language_add') }}">
                        <span class="icon"><i class="fa fa-add"></i></span>
                    </a>
                </th>
            </tr>
            </thead>
            <tbody>
            {% for language in languages %}
                <tr>
                    <td>
                        <img src="{{ asset('images/flags/' ~ language.code ~ '.svg') }}" width="32" height="24" alt="{{ language.code }}"/>
                    </td>
                    <td>{{ language.code }}</td>
                    <td>{{ language.name }}</td>
                    <td>
                        {% if language.tileImage %}
                            <span class="has-text-success">{{ 'global.button_yes'|trans }}</span>
                        {% else %}
                            <span class="has-text-grey">{{ 'global.button_no'|trans }}</span>
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('app_admin_language_toggle', {'id': language.id}) }}"
                           title="{{ language.enabled ? 'admin_system.button_disable'|trans : 'admin_system.button_enable'|trans }}">
                            {% if language.enabled %}
                                <span class="tag is-success">{{ 'admin_system.status_enabled'|trans }}</span>
                            {% else %}
                                <span class="tag">{{ 'admin_system.status_disabled'|trans }}</span>
                            {% endif %}
                        </a>
                    </td>
                    <td>{{ language.sortOrder }}</td>
                    <td>
                        <a href="{{ path('app_admin_language_edit', {'id': language.id}) }}">
                            <span class="icon"><i class="fa fa-edit"></i></span>
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>

{% endblock %}
