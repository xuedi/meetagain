{#
    Gallery: Sidebar concept -- Abstract reference for guest-facing sidebar columns

    The backend deliberately avoids prescribing one sidebar look. Pages that benefit
    from a right-hand guidance column (auth, support, signup-style pages) compose it
    from existing Bulma `column` markup. The live frontend reference is `/groups/create`.

    Used in:    admin/system/theme/gallery/index.html.twig
    Parameters: none
#}
<p class="is-size-7 has-text-grey">
    {{ 'admin_system_gallery.source_of_truth'|trans }}
    <code>templates/groups/create.html.twig</code>
</p>

<p>{{ 'admin_system_gallery.sidebar_concept_body'|trans }}</p>

<p class="mt-3">
    <a href="/groups/create" target="_blank" rel="noopener">
        {{ 'admin_system_gallery.sidebar_concept_live_link'|trans }}
    </a>
</p>
