{% extends 'admin/base.html.twig' %}

{% block content %}

    <div class="container">

        {% include 'admin/email/_tabs_navigation.html.twig' with {'activeSection': 'sendlog'} %}

        {% if providerAvailable %}
            <div class="mb-4">
                <form method="post" action="{{ path('app_admin_email_sendlog_sync') }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('sync') }}">
                    <button type="submit" class="button is-small is-info">
                        <span class="icon"><i class="fa fa-refresh"></i></span>
                        <span>Sync from Provider</span>
                    </button>
                </form>
            </div>
        {% endif %}

        <table id="filteredTable" class="table is-fullwidth">
            <thead>
            <tr>
                <th>Created</th>
                <th>Recipient</th>
                <th>Subject</th>
                <th>Status</th>
                <th>Provider Status</th>
                <th>Template</th>
                <th>Sent At</th>
            </tr>
            </thead>
            <tbody>
            {% for email in emails %}
                <tr>
                    <td>
                        {{ email.createdAt|date('Y-m-d') }}<br>
                        <small class="has-text-grey">{{ email.createdAt|date('H:i:s') }}</small>
                    </td>
                    <td>{{ email.recipient }}</td>
                    <td>{{ email.subject }}</td>
                    <td>
                        {% if email.isFailed() %}
                            <span class="icon-text has-text-danger">
                                <span class="icon"><i class="fa fa-exclamation-triangle"></i></span>
                                <span>failed</span>
                            </span>
                            {% if email.errorMessage %}
                                <br><small class="has-text-grey">{{ email.errorMessage|u.truncate(50) }}</small>
                            {% endif %}
                        {% elseif email.isSent() %}
                            <span class="has-text-success">sent</span>
                        {% else %}
                            <span>pending</span>
                        {% endif %}
                    </td>
                    <td>
                        {% set ps = email.providerStatus %}
                        {% if ps == 'delivered' %}
                            <span class="tag is-success">delivered</span>
                        {% elseif ps == 'undelivered' %}
                            <span class="tag is-danger">undelivered</span>
                        {% elseif ps == 'queued' %}
                            <span class="tag is-warning">queued</span>
                        {% elseif ps is not null %}
                            <span class="tag is-light">{{ ps }}</span>
                        {% elseif email.providerMessageId %}
                            <span class="has-text-grey">pending sync</span>
                        {% else %}
                            <span class="has-text-grey">–</span>
                        {% endif %}
                    </td>
                    <td>
                        {% if email.template %}
                            <span class="tag is-light">{{ email.template.value }}</span>
                        {% else %}
                            <span class="has-text-grey">-</span>
                        {% endif %}
                    </td>
                    <td>
                        {% if email.sendAt %}
                            {{ email.sendAt|date('Y-m-d H:i:s') }}
                        {% else %}
                            <span class="has-text-grey">-</span>
                        {% endif %}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7" class="has-text-centered has-text-grey">
                        No email entries found.
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>

{% endblock %}
