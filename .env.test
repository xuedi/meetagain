# Test environment configuration
# Uses the same database as dev for functional tests

# Override the database to use the dev database instead of _test suffix
DATABASE_URL="mysql://meetAgain:UserPassW0rd@ma-db:3306/meetAgain?charset=utf8mb4"